#include <stdio.h>
int main (int argc, char *argv[]){
    struct PatientData {
        char name[20];  //patients's first name
        int age;        //Patient's age in years
        float weight;   //Patient's name in pounds
    };

    struct PatientData tim;
}